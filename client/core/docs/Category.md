# categories
the calumns must be:ID, Name, color, ApplyColorToAllItems (which is a boolean type )
implement this in both backend and frontend and for technical reference you can look other services we created before